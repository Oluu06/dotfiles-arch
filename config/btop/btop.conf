#############################################
# btop Configuration
# Catppuccin Mocha Theme
#############################################

# Color theme
color_theme = "catppuccin_mocha"

# Theme background
theme_background = True

# Truecolor
truecolor = True

# Force tty mode
force_tty = False

# Presets
presets = "cpu:1:default,proc:0:default cpu:0:default,mem:0:default,net:0:default cpu:0:block,net:0:tty"

# Vim keys
vim_keys = True

# Rounded corners
rounded_corners = True

# Graph symbol
graph_symbol = "braille"

# Graph symbol for CPU graph
graph_symbol_cpu = "default"

# Graph symbol for memory graph
graph_symbol_mem = "default"

# Graph symbol for network graph
graph_symbol_net = "default"

# Graph symbol for process graph
graph_symbol_proc = "default"

# Show battery
show_battery = True

# Selected battery
selected_battery = "Auto"

# Log level
log_level = "WARNING"

# Update time in milliseconds
update_ms = 2000

# Processes sorting
proc_sorting = "cpu lazy"

# Reverse sorting order
proc_reversed = False

# Show process tree
proc_tree = False

# Tree depth
tree_depth = 3

# Aggregate processes
proc_aggregate = False

# Processes per CPU
proc_per_core = False

# Show process memory in bytes
proc_mem_bytes = True

# Show process info box
proc_info_smaps = False

# Show process GPU info
proc_left = False

# CPU graph lower bound
cpu_graph_lower = "total"

# CPU graph upper bound
cpu_graph_upper = "total"

# Show CPU temperature
show_cpu_freq = True

# CPU sensor
cpu_sensor = "Auto"

# Show core map
show_coretemp = True

# CPU temperature scale
temp_scale = "celsius"

# Base 10 for disk
base_10_disk = False

# Show disk IO
show_io_stat = True

# IO mode
io_mode = False

# IO graph combined
io_graph_combined = False

# IO graph speeds
io_graph_speeds = ""

# Network graph auto
net_auto = True

# Network graph download
net_download = "10M"

# Network graph upload
net_upload = "10M"

# Network interface
net_iface = ""

# Show network disconnected
net_sync = True

# Show network color
net_color_fixed = False

# Show init
show_init = False

# Check temperature
check_temp = True

# Show GPU
show_gpu_info = "Auto"

# GPU mirror graph
gpu_mirror_graph = True

# Custom CPU name
custom_cpu_name = ""

# Disks filter
disks_filter = ""

# Memory graph mode
mem_graphs = True

# Memory graph mode
mem_below_net = False

# Show swap
show_swap = True

# Swap as disk
swap_disk = True

# Show disks
show_disks = True

# Only physical
only_physical = True

# Use fstab
use_fstab = True

# ZFS arc in memory
zfs_arc_cached = True

# Show disk free
show_disk_io = True

# Disk IO mode
disk_io_mode = False

# ZFS hide datasets
zfs_hide_datasets = False

# Show uptime
show_uptime = True

# Show battery
show_battery = True

# Selected name
selected_name = "Auto"

# Background update
background_update = True

# Update check
update_check = False

# Show processes
proc_filter_kernel = False
